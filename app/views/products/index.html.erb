<h1>Lista de productos</h1>

<p>Hay <%=@products.count%> productos </p>

<table>
	<tr>
		<th> Referencia	</th>
		<th> Marca </th>
		<th> Descripción </th>	
		<th> Acciones </th>	
	</tr>
	<% @products.each do |p|%>
		<tr>
			<td><%= p.ref%></td>
			<td><%= p.brand%></td>
			<td><%= p.description%></td>
			<td><%= link_to 'Ver detalle', url_for(controller: :products, action: :show,id: p.id)%></td>
			<td><%= link_to 'Editar',url_for(controller: :products,action: :edit, id: p.id)%>||</td>
			<td><%= link_to 'Borrar',p,id: p.id, method: :delete,data:{confirm: "En serio lo quiere borrar?"}%></td>
		</tr>

	<% end %>
</table>
